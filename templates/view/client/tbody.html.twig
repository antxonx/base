{% set tr = {} %}
{% for client in clients %}
    {# suspneder o reactivar #}
    {% set trashSusp %}
        {% if client.suspended %}
            <button class="btn btn-sm btn-outline-dark border-0 w-100 client-reactivate mobile-2-desktop-1"><i class="fas fa-redo-alt"></i></button>
        {% else %}
            <button class="btn btn-sm btn-outline-dark border-0 w-100 client-delete mobile-2-desktop-1"><i class="fas fa-trash-alt"></i></button>
        {% endif %}
    {% endset %}
    {# . #}
    {% set tr = {
        values : {
            id : {
                value : client.id,
                attr : {
                    class : 'client-show cursor-pointer text-right',
                }
            },
            name : {
                value : client.name,
                attr : {
                    class : 'client-show cursor-pointer text-nowrap',
                }
            },
            phone : {
                value : '<a class="btn btn-sm w-100 round alert-info text-center" href="tel:' ~ client.getClientExtras(2)[0].value ~ '">' ~ client.getClientExtras(2)[0].value ~ '</a>',
            },
            email : {
                value : '<a class="btn btn-sm w-100 round alert-info text-center" href="mailto:' ~ client.getClientExtras(1)[0].value ~ '">' ~ client.getClientExtras(1)[0].value ~ '</a>',
            },
            trash : {
                value : trashSusp,
                attr : {
                    class : 'text-center action-btn'
                }
            }
        },
        attr : {
            class : 'client-row',
            id: client.id,
            name: client.name
        }
    } %}
    {% include "table/tr.html.twig" with {
        tr : tr
    } %}
{% endfor %}
{% if tr.values is defined %}
    {% include "paginatorTable.html.twig" with {
        items: clients,
        cols : tr.values|length
    } %}
{% endif %}

