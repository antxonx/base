/**
* dashboard view
* @packageDocumentation
* @module Dashboard
* @preferred
*/
import '@components/simplecard';
import Dashboard from "@scripts/dashboard/dashboard";

(() => {
    (new Dashboard()).load();
})();
